extends ../base.pug

block content 
  video(src="/" + video.fileUrl controls width=300 height=500)
  //- div 
  //-   p=video.description
  //-   p=video.hashtags
  //-   small=video.createdAt
  //- div 
  //-   small Uploaded by 
  //-     a(href=`/user/${video.owner._id}`)=video.owner.name
  //- hr
  //- if String(video.owner._id) === loggedInUser._id
  //-   a(href=`${video.id}/edit`) Edit
  //-   br
  //-   a(href=`${video.id}/delete`) Delete

block scripts 
  script(src="/assets/js/videoPlayer.js") 