extends ../base.pug

block content 
  div#videoContainer
    video(src="/" + video.fileUrl width="300" hegith="300")
    div 
      button#play Play 
      button#mute Mute 
      input(type="range" min="0" max="1" step="any")#volume
      div 
        span#currentTime 00:00
        span  / 
        span#totalTime 00:00
      div
        input(type="range" min="0" value="0" step="1")#timeline
      div 
        button#fullScreen Enter Full Screen
  //- div 
  //-   p=video.description
  //-   p=video.hashtags
  //-   small=video.createdAt
  //- div 
  //-   small Uploaded by 
  //-     a(href=`/user/${video.owner._id}`)=video.owner.name
  //- hr
  //- if String(video.owner._id) === loggedInUser._id
  //-   a(href=`${video.id}/edit`) Edit
  //-   br
  //-   a(href=`${video.id}/delete`) Delete

block scripts 
  script(src="/assets/js/videoPlayer.js") 